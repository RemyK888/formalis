import { IFormalisSet } from '../../core/interfaces/IFormalisSet';
import { Predicate } from '../../types';

/**
 * Stub for breadth-first enumeration (placeholder).
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function* breadthFirstEnumeration<T>(_set: IFormalisSet<T>, _predicate?: Predicate<T>): Iterable<T> {
  // Not implemented — to be replaced with a true strategy.
  yield;
}
